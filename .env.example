# URL base de la API del backend.
VITE_API_BASE_URL=http://localhost:5000/api/v1

# Mientras el backend no esté disponible: usar datos simulados (mock) en el frontend.
VITE_USE_MOCK=false

# TEMPORAL (Sprint 1): id del docente simulado, mientras no exista login real (HU-015,
# Sprint 2). Debe coincidir con el id_usuario del docente sembrado en seed.sql del backend.
# Verificar con: SELECT id_usuario, nombre_completo, rol FROM usuario WHERE rol = 'docente';
VITE_DOCENTE_ID_SIMULADO=1